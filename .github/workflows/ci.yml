# CI — jakosc na push do main i na Pull Requestach. Testy (lint + test + build)
# sa w reusable workflow tests.yml, wspoldzielonym z deploy.yml — tu tylko je wolamy.
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/tests.yml
